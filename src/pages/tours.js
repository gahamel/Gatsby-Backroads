import React, { Component } from 'react'
import Layout from '../components/Layout';
import Button from '../examples/Button';

export default class tours extends Component {
  render() {
    return (
      <Layout>
        Hello from Tour pages!
        
        <Button>Styled button</Button>
      </Layout>
    )
  }
}
